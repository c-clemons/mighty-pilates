# The empirica_core package is vendored into this repo
# (run ~/empirica-core/scripts/vendor_into.sh <this-repo> to refresh it).
# Only runtime deps are listed here.
# Upper bounds pin the tested majors (pandas 3.0 / numpy 2.x+ have breaking changes).
streamlit>=1.37,<2
pandas>=2.2,<3
numpy>=1.26,<3
plotly>=5.18,<7
openpyxl>=3.1,<4
google-cloud-storage>=2.10,<4
